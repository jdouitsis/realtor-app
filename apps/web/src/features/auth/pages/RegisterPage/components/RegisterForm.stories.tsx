import type { Meta, StoryObj } from '@storybook/react-vite'

import { RegisterForm } from './RegisterForm'

const meta: Meta = {
  title: 'Features/Auth/RegisterPage/RegisterForm',
  component: RegisterForm,
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj

export const Default: Story = {}
